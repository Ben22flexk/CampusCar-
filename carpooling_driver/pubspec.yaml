name: carpooling_driver
description: "CampusCar - Driver App for TARC"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase & Auth
  supabase_flutter: ^2.10.1
  app_links: ^3.5.0
  
  # State Management
  hooks_riverpod: ^2.5.1
  flutter_hooks: ^0.20.5
  
  # UI & Icons
  cupertino_icons: ^1.0.8
  
  # Maps & Location
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^14.0.2
  geocoding: ^3.0.0
  mqtt_client: ^10.0.0
  
  # Utilities
  intl: ^0.19.0
  equatable: ^2.0.5
  http: ^1.2.0
  
  # Push Notifications
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1
  
  # Media & Permissions
  image_picker: ^1.0.7
  permission_handler: ^12.0.1
  url_launcher: ^6.2.5
  google_maps_flutter: ^2.14.0
  
  # PDF & Reports
  pdf: ^3.11.1
  printing: ^5.13.4
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
